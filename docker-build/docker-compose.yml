services:
  builder:
    build: .
    container_name: maven-node-dev
    user: 1000:1000
    volumes:
      - ..:/app
      - maven-repo:/root/.m2
    command: ["bash", "-c", "docker-build/build.sh"]

volumes:
  maven-repo: